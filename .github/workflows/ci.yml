name: CI - Frontend

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  ci:
    uses: nohaila1231/template-pro/.github/workflows/ci.yml@main
    with:
      node-version: '22'
      run-tests: false
